import React from 'react';

const Reservations = () => {
  const allReservations =
  // need to fetch reservations in app and pass them down

  return (
    <section>
      { allReservations }
    </section>
  );
}
